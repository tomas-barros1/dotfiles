input type:keyboard {
    xkb_layout "us"
    xkb_variant "intl"
}

input type:pointer {
    accel_profile flat
    pointer_accel 0
}

input type:touchpad {
    tap enabled
    natural_scroll enabled
}
